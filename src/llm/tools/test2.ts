/**
 * This is a second test function
 *
 * @param {number} a The first number
 * @param {number} b The second number
 * @returns {number} The difference of both numbers
 */
export function subtract(a: number, b: number): number {
  return a- b;
}
